name: devdatapoint
description: DevDatapoint Flutter app
publish_to: 'none'

version: 1.0.2+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3
  in_app_purchase: ^3.2.0
  url_launcher: ^6.3.0
  intl: ^0.19.0
  file_picker: ^8.0.7

  supabase_flutter: ^2.5.6
  uuid: ^4.4.0

  firebase_core: ^2.30.0
  firebase_messaging: ^14.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/